// Int | 100
union LeftOrRight {
    left(i32),
    right(i32),
}

let num = LeftOrRight { left: 0 };
let result = case num {
    left(value) => {
        100
    }
    right(value) => {
        -100
    }
};
result
