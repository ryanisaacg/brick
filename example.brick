import stdio

stdio.print("I will double your numbers");
let x = stdio.read();
while x > 0 {
    stdio.write(x * 2);
    x = stdio.read();
}

interface Point {
    x: f32,
    y: f32,
}

fn move(point: Point): f32 {
    point.x + point.y
}

