extern fn connect(): bool;

struct Point {
    x: f32,
    y: f32,
}

fn test() {
    if connect() {
        1 + 1;
    }
}

fn distance_2(a: Point, b: Point): f32 {
    let xd = a.x - b.x;
    let yd = a.y - b.y;

    (xd * xd) + (yd * yd)
}

if connect() {
    distance_2(Point { x: 1.0, y: 1.0 }, Point { x: 3.0, y: 1.0 })
} else {
    1.0
}
