interface HasArea {
    fn area(area: HasArea): i32,
}

struct Square {
    size: i32,

    fn area(self: ref Square): i32 {
        self.size
    }
}

let y = Square { size: 5 };
let x: HasArea = y;
x.area()
